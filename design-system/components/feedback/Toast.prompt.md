Toast — transient mono confirmation at the bottom: "SAVED · 16:04". Success has no color: ink strip, plain words.

```jsx
<Toast open={saved} onDismiss={()=>setSaved(false)}>Attendance saved · 8 / 10</Toast>
```
