StatusChip — every status everywhere: bookings, orders, waivers, sessions. Swatch = color, text = meaning.

```jsx
<StatusChip status="ACTIVE" /> <StatusChip status="NEEDS RE-CONSENT" /> <StatusChip status="PAID" tone="field" />
```

Never color the text itself; never invent per-status text colors.
