EmptyState — a mono sentence, not an illustration. State the fact, offer the next step.

```jsx
<EmptyState ticks action={<Button variant="secondary" size="sm">Add a session</Button>}>
  No sessions on this day — courts rest on Wed & Fri
</EmptyState>
```
