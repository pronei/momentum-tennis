# Development — public, non-secret values only (committed).
# Secrets go in .env.local (gitignored). The deployed dev project
# (momentumtennis-dev) overrides PUBLIC_SITE_URL via CF project env.
PUBLIC_SUPABASE_URL=TODO_dev_supabase_project_url
PUBLIC_SUPABASE_ANON_KEY=TODO_dev_publishable_key
PUBLIC_SITE_URL=http://localhost:5173
EMAIL_FROM="Momentum Tennis (dev) <onboarding@resend.dev>"
