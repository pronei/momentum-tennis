/** Section eyebrow / kicker label — the "speaking voice": uppercase, 13px, tracked 0.107em. */
export interface EyebrowProps {
  children: React.ReactNode;
  /** True on court-blue fields */
  onField?: boolean;
  /** Lead with the frame-ticks micro-device */
  ticks?: boolean;
  style?: React.CSSProperties;
}
