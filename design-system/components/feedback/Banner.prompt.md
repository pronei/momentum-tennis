Banner — inline strip for states that belong to the page or form, not a single field.

```jsx
<Banner tone="error" action={<Button variant="secondary" size="sm">Review & sign</Button>}>
  Dev R. needs re-consent — liability waiver V3. Booking is paused.
</Banner>
<Banner>Times shown are Pacific. The academy sets class times each season.</Banner>
```
