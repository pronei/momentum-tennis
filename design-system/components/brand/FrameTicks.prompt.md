Frame-ticks — the small repeating device (4 cool squares + 1 amber). Use as a list marker, an eyebrow accent, a section divider, or a loading state. Squares, never circles (circles belong to the ball).

```jsx
<FrameTicks />
<FrameTicks loading />           {/* loading state */}
<FrameTicks tone="field" size={6} />
```
