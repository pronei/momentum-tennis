TimeField — times are data: mono 24h (16:00), never "4 pm". Arrows step ±15 min; ▾ lists court-hour slots.

```jsx
<TimeField label="Start" defaultValue="16:00" />
<TimeField label="End" defaultValue="17:30" error="end must be after start" />
```
