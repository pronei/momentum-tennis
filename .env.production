# Production — public, non-secret values only (committed).
# Used by the momentumtennis Cloudflare project (deploy/live).
# Secrets are set on that project via wrangler/dashboard, never here.
PUBLIC_SUPABASE_URL=TODO_prod_supabase_project_url
PUBLIC_SUPABASE_ANON_KEY=TODO_prod_publishable_key
PUBLIC_SITE_URL=https://momentum-tennis.com
EMAIL_FROM="Momentum Tennis <no-reply@momentum-tennis.com>"  # requires Resend domain verification
