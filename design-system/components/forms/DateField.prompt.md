DateField — dates are data: mono ISO display (2026-09-12), never locale prose. ▾ opens a month grid (portal calendar pattern); typing the ISO date is equally valid.

```jsx
<DateField label="Start date" defaultValue="2026-09-12" help="YYYY-MM-DD" />
```
